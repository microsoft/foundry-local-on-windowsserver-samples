<UserControl x:Class="PatientSummaryTool.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:PatientSummaryTool"
             xmlns:resources="clr-namespace:PatientSummaryTool.Properties"
             xmlns:vms="clr-namespace:PatientSummaryTool.ViewModels"
             xmlns:converters="clr-namespace:PatientSummaryTool.Utils.Converters"
             xmlns:views="clr-namespace:PatientSummaryTool.Views"
             xmlns:behaviors="clr-namespace:PatientSummaryTool.Utils.Behaviors"
             xmlns:localSharedControls="clr-namespace:PatientSummaryTool.Views.SharedUserControls"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBlock">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:CallMethodAction TargetObject="{Binding}" MethodName="LoadWelcome" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5,20,5,5">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" Text="{x:Static resources:Resources.WelcomePageTitle}" FontSize="25" FontWeight="Bold">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{StaticResource {x:Static SystemColors.WindowTextColorKey}}" />
                </TextBlock.Foreground>
            </TextBlock>
        </Grid>
        <Grid Grid.Row="1" Margin="5,15,5,5">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" Text="{x:Static resources:Resources.Introduction}" FontSize="15" FontWeight="DemiBold" Foreground="Gray"/>
        </Grid>
        <Grid Grid.Row="2" Margin="5,15,5,5">
            <Image HorizontalAlignment="Center" VerticalAlignment="Top" Source="/ImageAssets/welcome.png"/>
        </Grid>
        <Grid Grid.Row="3" Margin="5,15,5,50">
            <Button Content="{x:Static resources:Resources.PatientLookupButtonText}" Command="{Binding PatientLookupCommand}" Style="{StaticResource PatientLookupButton}" />
        </Grid>
    </Grid>
</UserControl>
